image: node:18

.shared: &shared
  tags:
    - docker
  only:
    - main
    - merge_requests
    - tags
    - release/*

.npm-job: &npm-job
  <<: *shared
  before_script:
    - export NODE_OPTIONS="--max-old-space-size=8192"
    - npm ci --cache .npm --prefer-offline

cache:
  key: $CI_COMMIT_REF_SLUG
  paths:
    - .npm/

stages:
  - check

prettier:
  stage: check
  <<: *npm-job
  script:
    - npm run check
